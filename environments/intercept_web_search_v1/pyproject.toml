[project]
name = "intercept-web-search-v1"
version = "0.1.0"
description = "intercept-web-search-v1 — rewrite Codex native web-search responses with @vf.intercept."
requires-python = ">=3.11"
dependencies = ["verifiers"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["intercept_web_search_v1"]
